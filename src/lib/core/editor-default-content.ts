import type { JSONContent } from "@tiptap/core"

/** Documento TipTap inicial al crear un proyecto — vacío por defecto. */
export const DEFAULT_EDITOR_JSON: JSONContent = {
  type: "doc",
  content: [],
}
